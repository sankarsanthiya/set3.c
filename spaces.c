#include <stdio.h>
#include<string.h>
int main()
{
   char a[100];
   int b,count=0,i;
   scanf("%s",a);
   b=strlen(a);
   for(i=0;i<b;i++)
   {
       if(s[i]==' ')
       count++;
       printf("%d",count);
       else
       printf("%d",count);
   }

    return 0;
}
